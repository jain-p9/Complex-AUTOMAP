train_path = "/content/train_images/"

test_path = "/content/test_images/"

mask_path = "/content/mask.npy"

saved_weights = "complex_automap_tiny_imagenet.hdf5"  #Model trained on tiny imagenet

final_weights = "complex_automap_25_cartesian_calcamp.hdf5"  #Final model file finetuned on calgary images 

batch_size = 100

epochs = 100
